version: '3'
services:
  client:
    container_name: client
    build:
      context: .
    ports:
      - 8080:8080
    volumes:
      - clientvol:/clientdata
    networks:
      - backend

networks:
  backend: {}

volumes:
  clientvol: {}
